name: Notify if Still Down

on:
  workflow_run:
    workflows: ["Uptime CI"]
    types:
      - completed
  workflow_dispatch:

jobs:
  notify-down:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: master # または main

      - name: Check for Down status and Notify
        env:
          WEBHOOK_URL: ${{ secrets.LARK_WEBHOOK_URL }}
        run: |
          # 大文字小文字を区別せず(-i) Downを探す
          if grep -i -q "Down" README.md; then
            echo "🚨 Down sites detected. Sending notification..."
            
            # 【重要】Larkの設定に合わせて 'Upptime' と 'Down' (英語) を含めます
            curl -X POST -H "Content-Type: application/json" \
            -d '{
              "msg_type": "text",
              "content": {
                "text": "⚠️ [Upptime Alert] 継続通知\n\nいくつかのサイトが現在も Down しています。\n\n詳細確認: https://github.com/${{ github.repository }}"
              }
            }' \
            $WEBHOOK_URL
          else
            echo "✅ All sites are up. No notification needed."
          fi
